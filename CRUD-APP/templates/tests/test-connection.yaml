apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CRUD-APP.fullname" . }}-test-connection"
  labels:
    {{- include "CRUD-APP.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CRUD-APP.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
